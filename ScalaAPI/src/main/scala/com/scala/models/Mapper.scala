package com.scala.models

object Mapper {

//  def convertEntityToModel(student: Student): StudentModel = {
//    new StudentModel(id = student.getId(), name = student.getName(), rollNo = student.getRollNo(), age = student.getAge())
//  }
//  def convertModelToEntity(studentModel: StudentModel): Student = {
//    val student = new Student();
//    student.setName(studentModel.name);
//    student.setAge(studentModel.age);
//    student.setRollNo(studentModel.rollNo);
//    student;
//  }

}
